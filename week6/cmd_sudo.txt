SPI

sudo raspi-config
창에서 : Interface Options → SPI → Enable
sudo reboot
까지 해둠

cmd 창에 작성해야 하는 것

sudo apt-get install python3-dev
git clone https://github.com/doceme/py-spidev.git

cd py-spidev
make all

• Python3 사용 시 Makefile 수정
PYTHON ?= python → PYTHON ?= python3 : 뒤에꺼로 


• 장치 파일 확인:
ls -al /dev
• 아래 파일이 있어야 정상적으로 활성화:

결과
spidev0.0
spidev0.1 



I2C
sudo raspi-config
창에서 : Interface Options → I2C→ Enable
sudo reboot
이것도 해둠



sudo apt-get install python3-dev

sudo apt-get install python3-smbus libi2c-dev
sudo reboot

ls -al /dev
아래와 비슷한 문구가 있으면 I²C 활성화 완료:
결과
crw-rw---- 1 root i2c 89, 1 Sep 22 15:00 i2c-1

굳이 안해도 될 둣??

테스트 툴 설치:
sudo apt-get install -y i2c-tools
i2cdetect -y 1

cd webapps
git clone https://github.com/adafruit/Adafruit_Python_BMP.git
cd Adafruit_Python_BMP
sudo python3 setup.py install  

설치 오류시 아래 명령어로 대체 실행
pip3 install adafruit-circuitpython-bmp280 
BMP180 주소 확인
i2cdetect -y 1

cd webapps
git clone https://github.com/adafruit/Adafruit_Python_SSD1306.git
cd adafruit_Python_SSD1306
sudo python3 setup.py install  

설치 오류시 아래 명령어로 대체 실행i2c
pip3 install adafruit-blinka adafruit-circuitpython-ssd1306 

OLED 주소 확인
i2cdetect -y 1

